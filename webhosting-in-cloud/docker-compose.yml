version: '3.8'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: webhosting-in-cloud
    ports:
      - "3000:3000"
    restart: always
    environment:
      - NODE_ENV=production
